
services:
  seasons:
    build: .
    container_name: seasons
    restart: unless-stopped
    ports:
      - "8040:8040"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    environment:
      - ENV=production
      - DATABASE_URL=/app/data/seasons.db
